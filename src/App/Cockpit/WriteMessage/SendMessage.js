import React from 'react'

const SendMessage = () => {
  const style = {
    fontWeight: 'bold',
    flex: 1,
  }

  return (
    <button
      style={style}
      type="submit"
    >
      Send
    </button>
  )
}

export default SendMessage
